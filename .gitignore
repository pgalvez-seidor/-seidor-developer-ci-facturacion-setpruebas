node_modules/
ui/node_modules/

# builds de electron — se generan localmente, no van al repo
dist/
ui/dist/

# credenciales — nunca al repo
.env.*
config/.clickup_token

# Lockfiles — se regeneran con npm install
package-lock.json
ui/package-lock.json

# Logs y artefactos de ejecución
npm-debug.log*
yarn-error.log*
# Evidencia y scripts temporales — ignorar excepto el orquestador
evidence/
!evidence/**/_autobot_run.spec.js
config/database.sqlite
config/state.properties
test-results/
app.log
*.log
*.sqlite-wal
*.sqlite-shm
